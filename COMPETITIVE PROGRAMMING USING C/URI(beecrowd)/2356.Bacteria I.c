#include<stdio.h>
int main()
{
    int i=0;
    char code[110];
    while(scanf("%s",code))
    {
        i+=1;
        if(i==1)
        {
            if(code[0]=='A'&&code[1]=='C'&&code[2]==[G]&&code[3]=='T')
        }
    }
}
